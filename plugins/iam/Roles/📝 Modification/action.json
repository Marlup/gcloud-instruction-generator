{
  "Update custom role": {
    "cmd": "gcloud iam roles update {role_id} --project={project_id} --add-permissions={permissions}",
    "params": [
      "role_id",
      "project_id",
      "permissions"
    ],
    "param_hints": {}
  }
}