{
  "Creates a new resource.": {
    "cmd": "gcloud dataplex tables create --lake={lake} --dataset-id={dataset_id} --table-id={table_id} --schema={schema}",
    "params": [
      "lake",
      "dataset_id",
      "table_id",
      "schema"
    ],
    "param_hints": {}
  }
}