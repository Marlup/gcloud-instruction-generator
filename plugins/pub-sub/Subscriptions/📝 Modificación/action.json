{
  "Updates an existing resource.": {
    "cmd": "gcloud pubsub subscriptions update {subscription} --ack-deadline={deadline}",
    "params": [
      "subscription",
      "deadline"
    ],
    "param_hints": {}
  },
  "Updates an existing resource. (variant)": {
    "cmd": "gcloud pubsub subscriptions update {subscription} --labels={labels}",
    "params": [
      "subscription",
      "labels"
    ],
    "param_hints": {}
  }
}