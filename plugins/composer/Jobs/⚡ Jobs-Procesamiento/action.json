{
  "Executes a SQL query.": {
    "cmd": "bq query --use_legacy_sql=false '{consulta}'",
    "params": [
      "consulta"
    ],
    "param_hints": {}
  },
  "Deletes the specified resource.": {
    "cmd": "bq load --source_format={formato} {dataset}.{tabla} gs://{ruta_archivo}",
    "params": [
      "dataset",
      "tabla",
      "formato",
      "ruta_archivo"
    ],
    "param_hints": {}
  }
}